config BR2_PACKAGE_PG_CONFIG
	bool "pg_config"
	depends on BR2_USE_MMU
	depends on !BR2_STATIC_LIBS
	help
	  PostgreSQL pg_config Binary

	  http://www.postgresql.org/docs/9.4/static

comment "libpq needs a toolchain w/ dynamic library"
	depends on BR2_STATIC_LIBS
